<%= content_tag(:div, '', data:{
  controller: "modal-window"
}) do %>
  <%= content_tag(:button, 'Make order', data:{
    action: "click->modal-window#showModal"
  }, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded", id: id) %>
<% end %>